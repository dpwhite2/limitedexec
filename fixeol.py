# Fixes newlines some files on Windows.  Some tools seem to output newlines # with two CR characters (and an LF character).  This script removes pairs of # CRs with a single CR.import sysdef main():    filename = sys.argv[1]        print 'Fixing file "{0}".'.format(filename)    f = open(filename, 'rb')    text = f.read()    f.close()        text = text.replace('\r\r','\r')        f = open(filename, 'wb')    f.write(text)    f.close()    main()